package sort.insert;
/**
 * 希尔排序
 *     1。思路:
 *          1.1 按照一定间隔（增量）将集合中的元素分为多个子集合。
 *              希尔增量一般是取自该序列（希尔增量序列）{n*(1/2),n*(1/2)*(1/2),n*(1/2)*(1/2)（1/2）},
 *              最优为增量序列为 Hibbard
 *          1.2 对子数组分别进行插入排序
 *          1.3 
 */
public class ShellSort {

}