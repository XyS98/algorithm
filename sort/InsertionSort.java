package sort;
import java.util.ArrayList;

/**
 * 插入排序
 * 1.思路:  
 *      从待排序的集合中拿取一个元素，插入到另一个有序的元素集合中,
 *      插入之前找到该元素在有序集合中的正确位置,使得插入新元素后集合依然有序
 */
public class InsertionSort {
    
    public class ArrayList<Integer> insertionSort(ArrayList<Integer> list){
        ArrayList<Integer> orderedList = new ArrayList<Integer>(); // 存放有序元素的链表
        for(int elem: list){

        }
    }
}